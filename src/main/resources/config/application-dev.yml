server:
  port: 80

spring:
  datasource:
    url: jdbc:postgresql://192.168.0.113:5432/kotlin
    username: root
    password: password
    driverClassName: org.postgresql.Driver
  devtools:
    restart:
      enabled

logging:
  level:
    jp:
      co:
        casareal: debug
